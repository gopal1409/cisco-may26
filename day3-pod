#lets crete our first pod 
   20  vi pod.yml
apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    ports:
    - containerPort: 80
##apply the file
   21  kubectl apply -f pod.yml
      ##wait for a minute till the pod show 1/1
   22  kubectl get pod
      ####describe pod check the event log of the pod
   23  kubectl describe pod nginx
#pod defination
#once we got the manifest file 
##to apply this manifest file we use kubectl apply -f nameoftheyaml file
##kubernetes read from top to bootom and left to rigt
#we are going to create an object inside your k8s cluster
apiVersion: v1 #the api version of the pod is v1
kind: Pod #the kind of the object is pod
metadata: #
  name: nginx #just give a naming convention to the pod
spec: #it is specification of the pod 
  containers: #we are creating a container inside the pod
  - name: nginx #this is the name of the contianer
    image: nginx:1.14.2 #image of the contianer
    ports:
    - containerPort: 80 #this is the port number which is used by the container to listen to the traffic
#lets create another pod with dry run
#this will show all the xommand you can execute using kubectl
 30  kubectl
#we want to create a pod kubectl run --help will show the pod creating stepos
   31  kubectl run --help
#this will do a dry run and generate the yaml file
   33  kubectl run my-first-pod --image=nginx --port 80 --dry-run=client -o yaml
   34  kubectl run my-first-pod --image=nginx --port 80 --dry-run=client -o yaml > pod2.yml
   35  cat pod2.yml
#apply it
   36  kubectl apply -f pod2.yml
#wait for a minute till the pod come back
   37  kubectl get pod
##lets deploy our custom image you need to replace the name of the image to your image name which you will get from hub.docker.com
 44  kubectl run my-custom-pod --image=gopal1409/skyapp --port 8080 --dry-run=client -o yaml > myapp.yml
   45  kubectl apply -f myapp.yml
cat myapp.yml
   46  kubectl get pod
#look into the describe pod this will show your custom image
   47  kubectl describe pod my-custom-pod
#lets delete all the pod 
 54  kubectl get pod
   55  kubectl get nodes
   56  kubectl delete pod nginx
   57  kubectl get pod
   58  kubectl delete pod my-first-pod
   59  kubectl delete pod my-custom-pod
   60  kubectl get pod

